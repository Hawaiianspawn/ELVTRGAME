# GDD Fleshing-Out Tracker

**Purpose:** a living checklist of what still needed writing/deciding in the design
docs, triaged **assignment-rubric-first → vertical-slice → full-v1**.

> **STATUS NOTE — re-verified 2026-07-31.** `docs/backlog/INDEX.md` (generated by
> `py Scripts/backlog.py reindex`) is the **live work queue** now — 120 filed tasks,
> ranked and lock-checked. This file is kept as the **assignment-scoped historical
> record**: what Part A's rubric asked for, and what Part B/C looked like on
> 2026-07-21. It is not re-triaged here; every genuinely open item below already has
> (or should get) its own `docs/backlog/task-*.md` entry, cited where one exists.
>
> **Part A is done — the assignment shipped, twice more since.** `docs/ASSIGNMENT-01-GDD-FIRST-DRAFT.md`
> was written, exported, and submitted as `docs/Emberkeep-GDD-Assignment-01.pdf`
> (21 Jul 2026, correctly under the *Emberkeep* working title in force that night).
> `docs/ASSIGNMENT-02-FINAL-GDD.md` superseded it as the Final GDD, submitted as
> `docs/Kindled-GDD-Assignment-02.pdf` (27 Jul 2026, under the current title, with the
> corrected win/loss framing below). `docs/Kindled-AgentCrew-Assignment-03.pdf`
> (27 Jul 2026) is a third, later deliverable. **The two 🔴 blockers this file
> originally flagged (no name, no win/loss) were both false by the time of that first
> submission** — see the corrected Part A below for the citations. Whether
> `docs/backlog/task-020` (which re-checks the PDF assembly specifically) should
> still be marked obsolete is that task's call, not this file's; the evidence is
> recorded here either way.

**Scoring constraint (drove Part A):** the GDD was submitted for **Assignment #1 —
First Draft of GDD**, due **21 July 2026, 11:59 PM ET**. Course:
*Multi-Agent AI for Game Development*. The rubric — not design completeness — was the
scoring driver. Key consequence: two required sections (**AI Architecture**,
**Technical Strategy**) are about the **development-time agent system** (the
`.claude/agents/*` subagents + `unreal-mcp` + `pixellab` MCP), described *through
their effect on this specific game*.

Source docs (all rich): `GDD.md`, `CLASSES.md`, `SYSTEMS.md`,
`docs/RTS-VERTICAL-SLICE.md`, `docs/SPIKE1-RESULTS.md`. **`WORLD.md` is superseded in
full** by the 2026-07-22 narrative reset (`docs/narrative/FLAME-FOUNDATION.md:1-9`) —
do not treat it as canon; it is kept only as an unedited historical record.

---

## Rubric → current-state gap map

| Criterion (pts) | Requires | Current state | Gap |
|---|---|---|---|
| **Game Specificity** /3.0 | Named game, core loop, **win/loss** | Core loop solid (GDD §3); name and win/loss both settled — `GDD.md:481`, `docs/ASSIGNMENT-02-FINAL-GDD.md:56-58` | ✅ Both resolved |
| **Player Experience Clarity** /2.5 | What player *sees/does* | Present but scattered across 4 docs | Assemble player-facing pass |
| **Agent Role Clarity** /2.0 | Each **dev agent** named + 1-sentence role | Agents exist in `.claude/agents/`; GDD never describes them | Missing section |
| **Technical Feasibility** /1.5 | Semester scope, ≥1 constraint, **token budget** | Great tech constraints (GDD §10); **no token budget / API constraints** | Missing |
| **Presentation** /1.0 | 4 sections present, single PDF | Content spread across many `.md` files | Assemble + export PDF |

**Anti-slop gate (from the brief):** generic/placeholder framing, or MAS concepts not
grounded in specific player-facing mechanics, = **zero** on Game Specificity + Player
Experience Clarity regardless of theoretical correctness. Every agent role must be tied
to a concrete thing the player sees or does.

> **This whole gap map is now stale** — it describes the state as of 2026-07-21. Every
> row it lists as missing was written and shipped; see Part A below for the corrected,
> cited version. Kept verbatim as the historical record of what the rubric audit found
> that night.

---

## Part A — Assignment-critical (shipped; kept as the historical record)

Organized by the four required sections + presentation. **Re-verified 2026-07-31: every
item below is done.** The two 🔴 items were corrected before the first submission, not
after — GDD.md had already re-decided both by 21 Jul.

### Executive Summary
- [x] Write a standalone Executive Summary. **Done** —
  `docs/ASSIGNMENT-01-GDD-FIRST-DRAFT.md:8-39` (§1, submitted as
  `docs/Emberkeep-GDD-Assignment-01.pdf`, 21 Jul); superseded by
  `docs/ASSIGNMENT-02-FINAL-GDD.md:34-58` (§1, submitted as
  `docs/Kindled-GDD-Assignment-02.pdf`, 27 Jul), which folds in the win/loss table below.

### Game Mechanics (player-facing actions + loop)
- [x] ~~🔴 **Name the game**~~ — **not a blocker at submission time.** `GDD.md:481`
  (§12 Q10) records the game as **Kindled**, re-decided 2026-07-27 (after Assignment #1
  shipped under the then-current title *Emberkeep*, which is why that PDF carries the
  old name — historically correct, not a leftover error). *Emberkeep* is the discarded
  working title; never reintroduce it as the name. Assignment #2's Final GDD
  (`docs/ASSIGNMENT-02-FINAL-GDD.md`) ships under *Kindled* throughout.
- [x] ~~🔴 **State the win/loss condition explicitly**~~ — **settled, and it matches the
  shipped code.** Per-run: `GDD.md:65` ("win or die, reset") and `:82`/`:349` ("floor
  boss / exit"). Stated as an explicit table in
  `docs/ASSIGNMENT-02-FINAL-GDD.md:56-58`: **loss = hero death, run ends immediately and
  finally; per-run win = survive the escalating waves and kill what the last one
  brings.** This matches the actual gate-1 implementation —
  `ELVTR/Source/ELVTR/Spike/Spike1GameMode.cpp:272-279` (hero death ends the run from any
  phase) and `:338-347` (clearing every entry in `WaveBroodCounts` enters `Won`) — and
  `docs/GATE1-FUN-PROTOTYPE.md:29-32` ("Hero death loses"). The old draft's *meta-win*
  clause ("accumulate world flags that weaken the Hollow Crown's grip") is dead —
  `WORLD.md`'s Hollow Crown and its 15 world flags were discarded by the 2026-07-22
  narrative reset, and meta-progression is now **deliberately unwritten**
  (`docs/narrative/FLAME-FOUNDATION.md:131-138`, `GDD.md:458` "Meta-progression
  (unwritten by choice)"). That's not an open item — it's a scoped-out one; do not draft
  a replacement meta-win clause without an owner decision.
- [x] Assemble the player-facing mechanics section. **Done** —
  `docs/ASSIGNMENT-01-GDD-FIRST-DRAFT.md:43-93` (§2); current version
  `docs/ASSIGNMENT-02-FINAL-GDD.md:197+` (§3, "What the Player Sees and Does").

### AI Architecture (dev agents, described via gameplay effect)
- [x] Agent table, each tied to a player-facing outcome. **Done** —
  `docs/ASSIGNMENT-01-GDD-FIRST-DRAFT.md:95-111` (§3, includes `narrative-director`,
  `pixel-art-director`, `gameplay-director`, the `unreal-mcp` and `pixellab` tool
  servers, the orchestrator, and the GDD review board — a superset of this checklist's
  four rows). Current version: `docs/ASSIGNMENT-02-FINAL-GDD.md:329+` (§4, "The
  Development Crew").

### Technical Strategy (agent roles, token budget, API constraints)
- [x] Agent-roles table, token budget, API constraints, semester scope. **Done** —
  `docs/ASSIGNMENT-01-GDD-FIRST-DRAFT.md:115-181` (§4), with a **measured** (not
  estimated) token budget off `scripts/token-usage-report.js`. Current version:
  `docs/ASSIGNMENT-02-FINAL-GDD.md:382+` (§5, "Scope and Constraints").

### Presentation
- [x] Assemble + export to PDF. **Done, three times over** —
  `docs/Emberkeep-GDD-Assignment-01.pdf` (21 Jul), `docs/Kindled-GDD-Assignment-02.pdf`
  (27 Jul, supersedes #1 per its own header), `docs/Kindled-AgentCrew-Assignment-03.pdf`
  (27 Jul, a later, separate deliverable). Proofreading is between the owner and the
  submitted PDFs, not re-auditable from this tracker.

---

## Part B — Vertical-slice design depth (re-verified 2026-07-31)

- [x] Fill `SYSTEMS.md`'s skeleton sections. **All five landed**, each as a design doc +
  schema-valid data file, each with a `docs/backlog/task-*.md` record:
  - [x] §1 entity tier stat blocks — `docs/backlog/task-002` done 2026-07-28 →
    `docs/design/entity-tiers.md`, `docs/data/entity-tiers.json`.
  - [x] §2 scaling curve across 3 floors — `docs/backlog/task-003` done 2026-07-28 →
    `docs/design/scaling-curve.md`, `docs/data/scaling-curve.json`. Since iterated on
    further by `docs/data/wave-scaling.json` (task-102/103, three-act wave scaling) —
    the base item is done, not frozen.
  - [x] §4 encounter budget table per floor — `docs/backlog/task-004` done 2026-07-29 →
    `docs/design/encounter-budget.md`, `docs/data/encounter-budget.json`.
  - [x] §6 Vanguard retinue tuning — `docs/backlog/task-005` done 2026-07-29 →
    `docs/design/retinue-tuning-vanguard.md`, `docs/data/retinue-vanguard.json`.
  - [x] loot v0 — `docs/backlog/task-006` done 2026-07-28 → `docs/design/loot-v0.md`,
    `docs/data/loot-v0.json`.
- [x] Create `docs/data/` and `docs/design/`. **Done** — both exist, well beyond the five
  items above (22 files under `docs/data/`, 15 under `docs/design/` as of this check).
  This line item in particular was itself stale in two places at once: `GDD-TODO.md`
  said they didn't exist, and `docs/backlog/task-018` had to separately correct the same
  claim inside the director agents' own canon lists.
- [ ] Fill `docs/SPIKE1-RESULTS.md` — **still genuinely open** (re-verified 2026-07-31).
  The file itself is still the blank template; `docs/backlog/task-007` (status
  `proposed`) owns running it. **But the risk this file exists to retire has already
  been measured, on a different/newer harness**, and the verdict is GO:
  `docs/perf/one-camera-bench.md` (measured 2026-07-28, standalone `-game`, the
  `-SwarmBench` config sweep) reports the GDD §10 entity-count gate **passed with a 7x
  margin** — 2.31ms / 433fps at the 1,000-unit gate on the Niagara sprite path (§1), a
  measured ~34,000-entity 60fps ceiling with `Swarm.SimLOD.Stride 4` (§5, §8) — superseding
  `docs/perf/BUDGETS.md`'s older debug-box-only numbers (which that file's own
  2026-07-28 header now flags as superseded). **`GDD.md:406-410`'s "the gate is still
  not passed" language is therefore itself stale** — `docs/perf/one-camera-bench.md:298`
  says as much ("Update GDD §10 — the gate is passed... the GDD is canon and is the
  owner's to change"). Out of scope for this file to fix (GDD.md edits aren't this
  task's to make); flagged here so whoever picks up task-007 or a GDD.md pass doesn't
  re-measure something already measured, and so the "no machine, no verdict" framing
  below doesn't get read as "nobody knows."
- [x] Art-test decisions — **both resolved, one by an actual test still pending, one by
  a bigger reversal that moots the question as originally framed**:
  - [ ] Flipbooks vs. flat-shaded 3D (GDD Q#5) — **still open.**
    `docs/backlog/task-015`, status `parked` (2026-07-28), pending an actual rendered
    side-by-side comparison. Lean is still flipbooks on instanced quads (`GDD.md:471`).
  - [x] Strict 4-color global vs. per-faction palettes (GDD Q#6) — **moot, not merely
    decided.** It was first resolved to strict global 4-value Demichrome on 2026-07-12
    (`docs/art/aesthetic-direction.md:39-53`), but that whole 2-bit posterize step was
    then **superseded 2026-07-28** — the game now ships in full colour,
    `Kindled.Quantize 0` (`Emberkeep.*` renamed to `Kindled.*` 2026-07-31, backlog
    tasks 092/093) (`docs/art/aesthetic-direction.md:8-20`; also recorded in
    memory `elvtr-game-design-decisions`). **`GDD.md:472`'s Q6 row is stale on both
    counts** — it still reads "Per-faction | Open," which was already wrong before the
    2026-07-28 reversal (`docs/backlog/task-016` exists specifically to write the first
    correction back into `GDD.md`, not yet done as of this check). Not fixed here — out
    of scope for this file — but flagged so the "gate all sprite production" framing
    below isn't read as still blocking on a 4-vs-per-faction choice nobody is making
    anymore.
- [x] RTS slice §4 design prerequisites — same five items as the `SYSTEMS.md` list
  above, all done; see there for citations (`docs/RTS-VERTICAL-SLICE.md:77-81` is the
  mirror list, now stale in its own unchecked-boxes but the underlying work is landed).

---

## Part C — Full-v1 backlog (re-verified 2026-07-31)

- [x] ~~**Base Camp Loot Manager**~~ — **CUT 2026-07-21** per the GDD review board
  (`gdd-review-kit`): reopened the §6a host-world rule for a stretch goal layered on
  a loot system that doesn't exist yet. Removed from `GDD.md` §8. Revisit only if/when
  the loot system *and* the Gatecamp hub are both real — this entry stays as the
  historical record of the idea, not an active backlog item.
- [ ] **Unwitnessed faction revisit** — still genuinely open/parked. `GDD.md:483` (§12
  Q13) keeps it "⏸ Parked — revisit before content lock"; `docs/backlog/task-033`
  (status `proposed`, recommends staying parked) adds that the first draft lives in
  `WORLD.md` §3a, which is superseded — whatever survives has to be *migrated* into
  flame canon, not resumed in place. Don't build titan content beyond Spike 1 until
  resolved (unchanged instruction).
- [ ] Loot & itemization system (GDD §8) — still deferred by design, and the deferral is
  now *stronger* than when this line was written: `GDD.md:473` (§12 Q7) records the
  deferral as "legitimate — retinue growth is the run reward loop, not loot," echoed by
  Q15 (`GDD.md:475`). `docs/backlog/task-034` (status `proposed`, recommends parking)
  scopes the sequencing: play loot v0 (Part B, done) first, revisit only if that play
  session says the game wants more.
- [ ] Naming pass — **split into one open item and one moot one:**
  - [ ] Final class names (CLASSES C1) — still open. `CLASSES.md:449` marks it "Working
    names set — revisit at naming pass"; `docs/backlog/task-032` (status `proposed`)
    tracks it.
  - [x] ~~Setting/hub/antagonist names (WORLD W1)~~ — **moot as originally framed.**
    `WORLD.md` is discarded in full. Its replacement, `docs/narrative/FLAME-FOUNDATION.md`,
    states plainly that naming the setting is not merely undone but **deliberately
    deferred behind two open design questions** (§4.1 "stand in the circle" stagnation,
    §4.4 what uniting a fire actually does) — "those get written once the prototype
    answers §4.1 and §4.4... writing them now would repeat exactly the mistake this
    reset exists to undo" (`docs/narrative/FLAME-FOUNDATION.md:131-138`). Not a naming
    task at all until those two questions resolve.
- [ ] S8 "The Silent Bell" world flag — **status uncertain, not just "design later."** Its
  source, `WORLD.md` §7, is fully superseded (2026-07-22 reset). `docs/backlog/task-035`
  (status `proposed`, recommends parking) is explicit that the honest state is "we do not
  currently know whether S8 still exists as canon at all," since the 15-flag world-state
  system it belongs to was part of the discarded canon and nothing has re-decided whether
  any version of it survives. Designing effects for it now risks re-importing superseded
  canon through the back door (the exact failure `docs/backlog/task-018` existed to stop
  elsewhere). Blocked on a bigger question than this line implies.
- [ ] Pacing director — L4D-style intensity manager (`SYSTEMS.md` §5). Still undesigned,
  but **its stated blockers are now cleared**, worth flagging since `docs/backlog/task-036`
  (status `parked` 2026-07-29) recommended parking specifically because it depends on the
  encounter budget (task-004) and run structure (task-024) — both filed as "don't exist
  yet" at the time. **Both are now `done`** (Part B above). The park may still be right —
  task-036 also notes Gate 1's fixed wave/breather rhythm might already be enough, and
  that's a playtest question (task-008), not a design one — but the dependency reason for
  waiting no longer holds as stated, so this is worth a fresh look rather than an
  automatic continued park.
- [ ] Remaining open questions — **CLASSES C2–C11 still open; WORLD W2–W7 moot; the GDD
  log is mostly closed:**
  - [ ] `CLASSES.md` C2–C11 — still open, not yet swept. `docs/backlog/task-037` (status
    `proposed`) exists to do exactly this sweep but was filed as parked-until-`task-018`
    lands (auditing canon before the canon pointers are fixed would audit the wrong
    canon). **`task-018` is now `done`** (2026-07-29, all five director agents repointed
    off `WORLD.md`) — so `task-037`'s blocker is also cleared, same as the pacing
    director above.
  - [x] ~~WORLD W2–W7~~ — **moot.** `WORLD.md` discarded in full, 2026-07-22
    (`docs/narrative/FLAME-FOUNDATION.md:1-9`). Not "open" — the document they belong to
    isn't canon.
  - [x] GDD Open Questions log (`GDD.md` §12) — **mostly closed already**; most rows read
    "✅ Decided." What's genuinely still open there duplicates the items above: Q5 (art
    test), Q11/C1 (final class names), Q13 (Unwitnessed). One stale row worth naming
    since it was found during this pass: **Q6 (`GDD.md:472`) still reads "Per-faction |
    Open,"** which was wrong even before the 2026-07-28 full-colour reversal (see Part B
    above) — `docs/backlog/task-016` owns that correction and hasn't landed it yet. Not
    fixed here; this file doesn't edit `GDD.md`.

---

*Tracker created 2026-07-21. Re-verified 2026-07-31 (`docs/backlog/task-001`) — see the
status note at the top of this file. `docs/backlog/INDEX.md` is the live work queue;
update checkboxes here only when correcting the historical record, not to track new
work — file a backlog task for that instead.*
