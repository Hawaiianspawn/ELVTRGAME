# 2-bit art specs

Deliverables from the `pixel-art-director` agent — palette tables, silhouette guides,
sprite-sheet layouts, animation notes. Specs only, no image files; the reference
pipeline is `ELVTR/SETUP-EDITOR.md` + `ELVTR/RawArt/T_Swarm_2bit.png` (48×48 cells,
SubUV sheets, Nearest filter, Unlit Masked). Input briefs live in `../briefs/`.
